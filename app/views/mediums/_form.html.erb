<% if @medium.errors.any? %>
    <h2>The following errors prevented the medium from being saved:</h2>
    <ul>
        <% @medium.errors.full_messages.each do |msg|%>
            <li><%= msg%></li>
        <% end %>
    </ul>
<% end %>


 <%= form_with(url: "/mediums", method: :post, data: {turbo: false}) do |f| %>

    <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
    </p>

    <p>
    <%= f.label :description %> <br>
    <%= f.text_area :description %> 
    </p>

    <p>
    <%= f.label :content_url %> <br>
    <%= f.text_area :content_url %> 
    </p>

    <!-- <p>
        <%= f.label :playlist_id %> <br>
        <%= f.collection_check_boxes :playlist_ids, Playlist.all, :id, :title do |b| %>
            <%= b.check_box %>
            <%= b.label %>
        <% end %>    
    </p>

    <p>
    <%= select "medium", "playlist_ids", Playlist.all.collect { |p| [p.title, p.id] }, { prompt: "Select playlists" }, { multiple: true } %>
    </p> -->

    <p>
    <%= f.submit %>
    </p>

<% end %>